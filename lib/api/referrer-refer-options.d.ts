import { RequestOptions } from "../core";
import { ReferrerDelegate } from "./referrer-delegate";
/**
 * Options for {@link Referrer.refer}.
 * @public
 */
export interface ReferrerReferOptions {
    /** See `core` API. */
    requestDelegate?: ReferrerDelegate;
    /** See `core` API. */
    requestOptions?: RequestOptions;
}
//# sourceMappingURL=referrer-refer-options.d.ts.map